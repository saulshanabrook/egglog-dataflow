# Option 3 Experiment Index

This index is generated from the numbered lane artifacts under
`findings/artifacts/option-3`. The lane ordering is dependency order:
schedule baseline, independent gates, dependent planner/scheduler/container
lanes, then integration verdicts.

| Branch | Key question | Artifact | Semantic | Freshness | Visibility | Barrier | Performance | Option implication |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 01-schedule-overlap | Can DD overlap physical work across logical schedule steps while preserving per-rule freshness? | `01-schedule-overlap.json` | pass | pass | 0 | 260 | 60 scaling runs, max runtime 577048 us, max frontier lag 3 tasks; wins are workload-dependent. | Strengthens the schedule-overlap subclaim, but only before real native barriers are added. |
| 02-rule-ir-classification | Which egglog rule shapes are fair join/planner targets versus rebuild/container/scheduler stressors? | `02-rule-ir-classification.json` | n/a | n/a | n/a | n/a | Static classification only; no runtime semantics measured in this lane. | Use binary and intersection shapes for join experiments; do not treat rebuild/container cases as WCOJ evidence. |
| 03-native-action-rebuild-barriers | Do native action and rebuild boundaries collapse overlap into hard visibility barriers? | `03-native-action-rebuild-barriers.json` | pass | pass | 0 | 4 | Five targeted native regression runs passed in 8559000 us total. | Adapter and hybrid DD/FlowLog designs should treat native rebuild/action behavior as an explicit barrier or handoff point. A replacement backend must model equivalent visibility boundaries internally. |
| 04-equality-rebuild-invalidation | Does native equality/rebuild invalidation preserve semantics on rebuild-heavy regressions? | `04-equality-rebuild-invalidation.json` | pass | pass | 0 | 0 | Three equality/rebuild checks passed in 1440569 us total. | Native rebuild semantics are sound on these cases, but DD integration needs explicit row-level invalidation counters. |
| 05-trace-progress | How much frontier lag and candidate flow does overlap introduce under scaling? | `05-trace-progress.json` | pass | pass | 0 | 260 | 60 scaling runs, max runtime 577048 us, max frontier lag 3 tasks, stale candidates 0. | Frontier lag is bounded in the current harness, but trace memory and compaction still need instrumentation. |
| 06-native-join-strategies | Do native egglog join strategies preserve results on representative join shapes? | `06-native-join-strategies.json` | n/a | n/a | n/a | n/a | Correctness smoke tests passed; no per-strategy runtime or stage-count table was exposed by the test harness. | Native planner strategies are available as correctness baselines; more instrumentation is needed to compare plan efficiency. |
| 07-wcoj-baselines | Can the checked-out WCOJ examples provide runtime baselines for triangle, four-clique, q6, and q7? | `07-wcoj-baselines.json` | n/a | n/a | n/a | n/a | Compile-only baseline; runtime probes are blocked on missing graph inputs. | WCOJ remains a follow-up component experiment; this checkout only proves the examples compile without a graph dataset. |
| 08-custom-scheduler-backoff | Do custom scheduler/backoff semantics require full-match admission boundaries? | `08-custom-scheduler-backoff.json` | pass | pass | 0 | 2 | Three scheduler/backoff runs passed in 5817000 us total. | Custom scheduler behavior remains native-boundary evidence; DD lowering must preserve materialization, admission, and delayed firing. |
| 09-container-dirty-refresh | Does same-id container dirty refresh preserve seminaive visibility? | `09-container-dirty-refresh.json` | pass | pass | 0 | 0 | Four container dirty-refresh checks passed in 1897907 us total. | Container refresh needs an equivalent first-class invalidation signal. The native dirty_ids split is oracle evidence for the behavior, not a required implementation shape for a replacement backend. |
| 10-real-barrier-survival | Do the real-barrier results reject an adapter framing or the new-backend framing? | `10-real-barrier-survival.json` | pass | pass | 0 | 6 | No integrated new-backend runtime exists yet; component runtimes are recorded in source lane artifacts. Synthetic native barriers reduce the adapter-style schedule harness to stop/start behavior. | Downgrade permanent middle-layer adapter duplication. Keep Option 3 open as a replacement-backend path if the next slice owns these barrier semantics without native shadow state. |
| 11-adapter-complexity-audit | Is the high Option 3 surface accidental adapter duplication or expected new-backend scope? | `11-adapter-complexity-audit.json` | n/a | n/a | n/a | n/a | Planner and WCOJ lanes are correctness/compile probes rather than full runtime evidence; the only measured throughput signal is still the schedule-overlap harness. | Do not build a permanent adapter that duplicates native state. A new backend remains viable if the next vertical slice owns this scope directly and uses native egglog only as an oracle. |

## Verdict Rules

- Exact overlap strengthens the schedule subclaim only when semantic equivalence, per-rule freshness, and visibility pass.
- Passing semantics with high native-authoritative barrier counts downgrades a permanent adapter, not a single-owner replacement backend.
- Any freshness or early-visibility failure rejects the exact-backend claim unless the failing case is explicitly out of scope.
- WCOJ/planner wins count as component evidence until they integrate with rebuild, scheduler, and container semantics.
